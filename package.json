{
  "name": "knowledge-compiler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=@knowledge-compiler/agent-contracts && npm run build --workspace=client && npm run build --workspace=server",
    "typecheck": "npm run typecheck --workspace=@knowledge-compiler/agent-contracts && npm run typecheck --workspace=client && npm run typecheck --workspace=server",
    "test": "npm run test --workspace=server",
    "migrate": "npm run migrate --workspace=server"
  },
  "engines": {
    "node": ">=20.11"
  }
}
