# Attention Mechanism

Attention uses query key value projections to compare tokens. An attention mask prevents attending to padding or future tokens, and softmax turns scores into weights for a weighted sum.
